# Asset Register Override

- Keep filters visibly labeled and grouped above the register.
- Show active-filter count and disable reset when no filters are active.
- Preserve the semantic table and provide horizontal scrolling on narrow screens.
- Keep the asset identity column sticky on mobile, rows compact, and actions stable at a minimum 38px target.
- Every icon-only action needs a specific accessible label and a visible tooltip.
- Destructive confirmation must focus the dialog, support Escape, restore focus, and show progress.
